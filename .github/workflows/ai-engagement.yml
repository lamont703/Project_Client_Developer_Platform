name: AI Community Engagement

on:
  schedule:
    # Run every 30 minutes
    - cron: '*/30 * * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  ai-engagement:
    runs-on: ubuntu-latest
    name: Trigger AI Community Engagement
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Trigger AI Engagement
        run: |
          echo "🤖 Triggering AI Community Engagement..."
          curl -X POST "https://qhlzjrcidehlpmiimmfm.supabase.co/functions/v1/api/monitoring/force-engagement" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json" \
            -w "\nHTTP Status: %{http_code}\n" \
            -s
          
      - name: Trigger Community Analysis
        run: |
          echo "📊 Triggering Community Analysis..."
          curl -X POST "https://qhlzjrcidehlpmiimmfm.supabase.co/functions/v1/api/monitoring/force-analysis" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json" \
            -w "\nHTTP Status: %{http_code}\n" \
            -s
          
      - name: Check Engagement Status
        run: |
          echo "📈 Checking Engagement Status..."
          curl -X GET "https://qhlzjrcidehlpmiimmfm.supabase.co/functions/v1/api/monitoring/stats" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json" \
            -w "\nHTTP Status: %{http_code}\n" \
            -s
